{
  "private": true,
  "dependencies": {
    "@bufbuild/buf": "^1.62.1",
    "@bufbuild/protobuf": "^2.10.2"
  },
  "scripts": {
    "generate": "buf generate --clean && buf generate --clean --template buf.gen.otlp.yaml",
    "dep-update": "buf dep update"
  }
}
